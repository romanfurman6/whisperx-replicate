# WhisperX RunPod Serverless Requirements
# Using PyTorch 2.2.0 for compatibility with pyannote.audio 2.1.1

# Core PyTorch stack - installed in Dockerfile
# torch==2.2.0
# torchvision==0.17.0
# torchaudio==2.2.0

# WhisperX - using main branch compatible with torch 2.1.x
git+https://github.com/m-bain/whisperX.git

# Pin pyannote versions for VAD compatibility (old checkpoint needs 2.x)
pyannote.audio==2.1.1
pyannote.pipeline==2.3
pyannote.core==5.0.0

# WhisperX core dependencies
# ctranslate2>=3.20.0
# faster-whisper>=0.9.0
# transformers>=4.30.0

# Audio processing
ffmpeg-python==0.2.0

# HTTP and async for multi-chunk processing
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.2.1

# RunPod serverless SDK
runpod>=1.6.0

# Cog for prediction interface compatibility
cog>=0.9.0
