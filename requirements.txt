# WhisperX RunPod Serverless Requirements
# Using working configuration from commit e247b26

# Core PyTorch stack - installed in Dockerfile
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# WhisperX - will auto-resolve pyannote dependencies
git+https://github.com/m-bain/whisperX.git

# Audio processing
ffmpeg-python==0.2.0

# HTTP and async for multi-chunk processing
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.2.1

# RunPod serverless SDK
runpod>=1.6.0

# Cog for prediction interface compatibility
cog>=0.9.0
