# WhisperX RunPod Serverless Requirements
# Using official WhisperX repo (handles all dependencies)

# WhisperX from main repo
git+https://github.com/m-bain/whisperX.git

# Ensure faster-whisper supports the model architecture
faster-whisper>=1.0.3

# Audio processing
ffmpeg-python==0.2.0

# HTTP and async for multi-chunk processing
requests>=2.31.0
aiohttp>=3.9.0
aiofiles>=23.2.1

# RunPod serverless SDK
runpod>=1.6.0

# HuggingFace fast download (Rust-based parallel downloads)
hf_transfer>=0.1.3
