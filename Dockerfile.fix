# Quick fix: Add cog to existing working image
FROM romanfurman/whisperx-runpod-serverless:v0.4

# Install cog
RUN pip install --no-cache-dir cog>=0.9.0

# Verify installation
RUN python3 -c "import cog; print(f'Cog installed: {cog.__version__}')"
RUN python3 -c "import runpod; print(f'RunPod installed: {runpod.__version__}')"
RUN python3 -c "import whisperx; print('WhisperX imported successfully')"

